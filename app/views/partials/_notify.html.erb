<% if !alert.blank? %>
    <script>
      $(document).ready(function () {
        $.notify({
          message: "<%= alert %>"
          },{
          type: 'danger'
          // newest_on_top: false,
          //   position: {
          //       from: "bottom",
          //       align: "right"
          //   },
          //   delay: 500000
        });
    });
    </script>
<% end %>

<% if !notice.blank? %>
  <script>
    $(document).ready(function () {
        $.notify({
        // $('.notification').notify({  
            message: "<%= notice %>"
        }, {
            type: 'success'
            // newest_on_top: false,
            // position: {
            //     from: "bottom",
            //     align: "right"
            // },
            // delay: 500000
        });
    });
  </script>
<% end %>